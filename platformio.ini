; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env]
monitor_speed = 115200
upload_speed = 921600
build_cache_dir = .pio/build_cache
; upload_protocol = espota
; upload_port = microframework.local
; upload_flags =
;    --port=3232
;    --auth=password

[env:esp32-s3-devkitm-1]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DSOFTWARE_VERSION=\"V1.0\"
    -DENABLE_DEFAULT_SECRETS=1 ; 0=secrets.default.h, 1=secrets.h
    -DLOG_LEVEL=0 ; 0=VERBOSE, 1=DEBUG, 2=INFO, 3=WARNING, 4=ERROR
    -DENABLE_LOGGING=1 ; 0=off, 1=on
    -DENABLE_BUTTON=1 ; 0=off, 1=on
    -DENABLE_EXAMPLE=1 ; 0=off, 1=on
    -DENABLE_LED=1 ; 0=off, 1=on
    -DENABLE_MQTT=1 ; 0=off, 1=on
    -DENABLE_NETWORK=1 ; 0=off, 1=on
    -DENABLE_TFT=1 ; 0=off, 1=on
    -DENABLE_TIMER=1 ; 0=off, 1=on

lib_deps =
    lovyan03/LovyanGFX@1.2.0
    fastled/FastLED@3.9.9
    mathertel/OneButton@2.6.1
    knolleary/PubSubClient@2.8